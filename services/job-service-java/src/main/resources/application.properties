# Application
spring.application.name=job-service

# Database (PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/cloudtask
spring.datasource.username=cloudtask
spring.datasource.password=cloudtask
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# RabbitMQ
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

# Actuator endpoints
management.endpoints.web.exposure.include=health,info,prometheus,metrics
management.endpoint.health.show-details=when-authorized
management.prometheus.metrics.export.enabled=true

# Histogram buckets for percentile queries in Prometheus
management.metrics.distribution.percentiles-histogram.http.server.requests=true

# Server
server.port=8080

# JWT
# Generate a secret with: openssl rand -base64 64
jwt.secret=9gGj/Xg0dZzsqACbfjzcBLaBdUCfnbkXOkNvL+0OA6DFxfexBIFe1gZDdhIpMn+ZXysQ2HvWErt99XaXq6TyXw==
jwt.expiration-ms=3600000
